diff -uNr linux-4.20.1/arch/x86/kernel/cpu/proc.c linux-4.20.1a/arch/x86/kernel/cpu/proc.c
--- linux-4.20.1/arch/x86/kernel/cpu/proc.c	2019-01-09 17:46:05.000000000 +0100
+++ linux-4.20.1a/arch/x86/kernel/cpu/proc.c	2019-01-10 19:44:41.336976050 +0100
@@ -112,7 +112,7 @@
 
 	seq_printf(m, "\nbogomips\t: %lu.%02lu\n",
 		   c->loops_per_jiffy/(500000/HZ),
-		   (c->loops_per_jiffy/(5000/HZ)) % 100);
+		   (c->loops_per_jiffy*HZ/5000) % 100);
 
 #ifdef CONFIG_X86_64
 	if (c->x86_tlbsize > 0)
diff -uNr linux-4.20.1/arch/x86/kernel/smpboot.c linux-4.20.1a/arch/x86/kernel/smpboot.c
--- linux-4.20.1/arch/x86/kernel/smpboot.c	2019-01-09 17:46:05.000000000 +0100
+++ linux-4.20.1a/arch/x86/kernel/smpboot.c	2019-01-10 19:44:41.338976093 +0100
@@ -603,7 +603,7 @@
 	pr_info("Total of %d processors activated (%lu.%02lu BogoMIPS)\n",
 		num_online_cpus(),
 		bogosum/(500000/HZ),
-		(bogosum/(5000/HZ))%100);
+		(bogosum*HZ/5000)%100);
 
 	pr_debug("Before bogocount - setting activated=1\n");
 }
diff -uNr linux-4.20.1/init/calibrate.c linux-4.20.1a/init/calibrate.c
--- linux-4.20.1/init/calibrate.c	2019-01-09 17:46:05.000000000 +0100
+++ linux-4.20.1a/init/calibrate.c	2019-01-10 19:44:41.340976136 +0100
@@ -307,7 +307,7 @@
 	if (!printed)
 		pr_cont("%lu.%02lu BogoMIPS (lpj=%lu)\n",
 			lpj/(500000/HZ),
-			(lpj/(5000/HZ)) % 100, lpj);
+			(lpj*HZ/5000) % 100, lpj);
 
 	loops_per_jiffy = lpj;
 	printed = true;
diff -uNr linux-4.20.1/kernel/Kconfig.hz linux-4.20.1a/kernel/Kconfig.hz
--- linux-4.20.1/kernel/Kconfig.hz	2019-01-09 17:46:05.000000000 +0100
+++ linux-4.20.1a/kernel/Kconfig.hz	2019-01-10 21:17:41.578315806 +0100
@@ -45,6 +45,24 @@
 	 1000 Hz is the preferred choice for desktop systems and other
 	 systems requiring fast interactive responses to events.
 
+	config HZ_1500
+		bool "1500 HZ (EXPERIMENTAL)"
+	help
+	 1500 Hz is an experimental choice for desktop systems and other
+	 systems requiring fast interactive responses to events.
+
+	config HZ_2000
+		bool "2000 HZ (EXPERIMENTAL)"
+	help
+	 2000 Hz is an experimental choice for desktop systems and other
+	 systems requiring fast interactive responses to events.
+
+	config HZ_10000
+		bool "10000 HZ (EXPERIMENTAL)"
+	help
+	 10000 Hz is an experimental choice for desktop systems and other
+	 systems requiring fast interactive responses to events.
+
 endchoice
 
 config HZ
@@ -53,6 +71,9 @@
 	default 250 if HZ_250
 	default 300 if HZ_300
 	default 1000 if HZ_1000
+	default 1500 if HZ_1500
+	default 2000 if HZ_2000
+	default 10000 if HZ_10000
 
 config SCHED_HRTICK
 	def_bool HIGH_RES_TIMERS
diff -uNr linux-4.20.1/net/netfilter/nf_conntrack_proto_sctp.c linux-4.20.1a/net/netfilter/nf_conntrack_proto_sctp.c
--- linux-4.20.1/net/netfilter/nf_conntrack_proto_sctp.c	2019-01-09 17:46:05.000000000 +0100
+++ linux-4.20.1a/net/netfilter/nf_conntrack_proto_sctp.c	2019-01-10 21:35:16.767752101 +0100
@@ -48,12 +48,12 @@
 	"HEARTBEAT_ACKED",
 };
 
-#define SECS  * HZ
+#define SECS  * 1ul * HZ
 #define MINS  * 60 SECS
 #define HOURS * 60 MINS
 #define DAYS  * 24 HOURS
 
-static const unsigned int sctp_timeouts[SCTP_CONNTRACK_MAX] = {
+static const unsigned long int sctp_timeouts[SCTP_CONNTRACK_MAX] = {
 	[SCTP_CONNTRACK_CLOSED]			= 10 SECS,
 	[SCTP_CONNTRACK_COOKIE_WAIT]		= 3 SECS,
 	[SCTP_CONNTRACK_COOKIE_ECHOED]		= 3 SECS,
diff -uNr linux-4.20.1/net/netfilter/nf_conntrack_proto_tcp.c linux-4.20.1a/net/netfilter/nf_conntrack_proto_tcp.c
--- linux-4.20.1/net/netfilter/nf_conntrack_proto_tcp.c	2019-01-09 17:46:05.000000000 +0100
+++ linux-4.20.1a/net/netfilter/nf_conntrack_proto_tcp.c	2019-01-10 21:34:24.253320111 +0100
@@ -64,12 +64,12 @@
 	"SYN_SENT2",
 };
 
-#define SECS * HZ
+#define SECS * 1ul * HZ
 #define MINS * 60 SECS
 #define HOURS * 60 MINS
 #define DAYS * 24 HOURS
 
-static const unsigned int tcp_timeouts[TCP_CONNTRACK_TIMEOUT_MAX] = {
+static const unsigned long int tcp_timeouts[TCP_CONNTRACK_TIMEOUT_MAX] = {
 	[TCP_CONNTRACK_SYN_SENT]	= 2 MINS,
 	[TCP_CONNTRACK_SYN_RECV]	= 60 SECS,
 	[TCP_CONNTRACK_ESTABLISHED]	= 5 DAYS,
